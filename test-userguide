1. Molecule
      pip install molecule 
      pip install ansible
      pip install docker
      
 2. Initialize the role  
    molecule init role -r app -d docker
    molecule init role -r geerlingguy.example -d docker
   
3. Test     
     molecule test 
